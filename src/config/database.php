<?php
return array(
    /*
    |--------------------------------------------------------------------------
    | Database Connection Name
    |--------------------------------------------------------------------------
    |
    | Here you may specify which of the configured database connections you wish
    | to use as the connection for all of Silverstripe's database work.
    |
    | To use whatever database connection is configured as the default, specify
    | null.
    |
    */
    'connection' => null,
);